// Futuro: restauración automática de fotografías antiguas (nitidez, color,
// daños). No implementado en esta etapa.

export async function restorePhoto(_photoId) {
  throw new Error("photoRestoration.service: no implementado todavía");
}
