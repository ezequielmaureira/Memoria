import { NotFoundState } from "../components/StateViews.jsx";

export function NotFoundPage() {
  return (
    <NotFoundState
      title="Página no encontrada"
      message="Esta página todavía no tiene memoria registrada."
    />
  );
}
